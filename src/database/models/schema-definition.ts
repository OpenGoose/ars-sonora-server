import { ENUMS_DEFINITION } from '@database/models/enums-definition';
import { TABLES_DEFINITION } from '@database/models/tables-definition';

export const users = TABLES_DEFINITION.users;
export const logs = TABLES_DEFINITION.logs;

// ENUMS
export const logType = ENUMS_DEFINITION.logType;
